{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {"type": "command", "command": "./vendor/bin/phpstan analyse --memory-limit=1G --no-progress"}
        ]
      }
    ]
  }
}
